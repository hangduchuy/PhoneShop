@model IEnumerable<ShopConnection.LoaiSanPham>
<div class="nav-item dropdown">
    <a href="#" class="dropdown-toggle nav-link" data-toggle="dropdown">Loại Sản Phẩm</a> @*M.Product la nha san xuat*@
    <div class="dropdown-menu">
        <div class="dropdown-inner">
            <ul class="list-unstyled">
                @foreach (var item in Model)
                {
                    <li style="list-style-type:none; "><a style=" color: #808080; padding-left:5px" href="@Url.Action("Index", "LoaiSanPham", new { id=@item.MaLoaiSP})">@item.TenLoaiSP</a></li>
                }
            </ul>
        </div>
    </div>
</div>